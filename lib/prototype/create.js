/**
 * Creates a new SimpleDocs singleton
 *
 * create a new instance
 * 
 * ####Example:
 * var simpledocs = require('simpledocs');
 * var sd1 = simpledocs.create('/docs').start();
 * var sd2 = simpledocs.create(path, modelName, modelOptions);
 * sd2.start('/more-docs');
 *
 * @param {String} text
 * @param {String} password
 * @method get
 * @api public
 */ 

module.exports = function(path, modelName, modelOptions) {
	
	return this;
};
